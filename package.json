{
  "name": "lox-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Rocketbook replacement monorepo",
  "scripts": {
    "dev:mobile": "pnpm --filter mobile-app start",
    "dev:api": "pnpm --filter api-server dev",
    "build": "pnpm -r build",
    "type-check": "pnpm -r type-check",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
